{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev & json-server --watch db.json --port 3001",
    "json-server": "json-server --watch db.json --port 3001",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.11.2",
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@element-plus/nuxt": "^1.0.8",
    "@nuxtjs/tailwindcss": "^6.11.4",
    "element-plus": "^2.6.3",
    "json-server": "^1.0.0-alpha.23",
    "sass": "^1.74.1"
  }
}
